spring.application.name=backend
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:stockflow}?useTimezone=true&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.password=${DB_PASSWORD:01241506}
spring.datasource.username=${DB_USERNAME:dandansousa}

jwt.expiration=900000
jwt.refresh-expiration=604800000

# Armazenamento de arquivos
AWS_S3_BUCKET=${AWS_S3_BUCKET:teste-864676094228-us-east-1-an}
AWS_S3_REGION=${AWS_S3_REGION:us-east-1}

# Limites de upload multipart
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB
