version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-stockflow
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: stockflow
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql